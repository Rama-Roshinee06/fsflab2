services:
  - type: web
    name: garden-of-time-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node clock-api.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
